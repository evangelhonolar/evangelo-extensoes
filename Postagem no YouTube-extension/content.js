// Auto-generated content script for replaying interactions
const ACTIONS = [
  {
    "attributes": {},
    "id": "1768508593819-jqnteiy1c",
    "selector": {
      "attributes": {},
      "css": "#upload-icon > yt-icon.remove-defaults > span.yt-icon-shape > div",
      "text": null,
      "xpath": "//*[@id=\"upload-icon\"]/yt-icon/span/div"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508593819,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508596444-ri6l35j5w",
    "selector": {
      "attributes": {},
      "css": "#select-files-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"select-files-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508596444,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {
      "name": "Filedata",
      "type": "file"
    },
    "id": "1768508596446-e21mj9bki",
    "selector": {
      "attributes": {},
      "css": "input[name=\"Filedata\"]",
      "text": null,
      "xpath": "//input[@name=\"Filedata\"]"
    },
    "tabId": 394884283,
    "tagName": "input",
    "text": "",
    "timestamp": 1768508596446,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {
      "name": "Filedata",
      "type": "file"
    },
    "id": "1768508609299-no4nzl1or",
    "inputType": "file",
    "selector": {
      "attributes": {},
      "css": "input[name=\"Filedata\"]",
      "text": null,
      "xpath": "//input[@name=\"Filedata\"]"
    },
    "tabId": 394884283,
    "timestamp": 1768508609299,
    "type": "input",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A",
    "value": "C:\\fakepath\\115.mp4" //usar o arquivo que está na planilha google na coluna "Nome do Arquivo de Video"
  },
  {
    "attributes": {
      "aria-label": "Adicione um título que descreva o conteúdo do vídeo (insira uma @ para mencionar um canal)",
      "role": "textbox"
    },
    "id": "1768508620921-277cyrlef",
    "selector": {
      "attributes": {},
      "css": "#textbox",
      "text": "", //usar o arquivo que está na planilha google na coluna "Titulo do Video"
      "xpath": "//*[@id=\"textbox\"]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508620921,
    "type": "input",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {
      "aria-label": "Fale sobre seu vídeo para os espectadores (insira uma @ para mencionar um canal)",
      "role": "textbox"
    },
    "id": "1768508626727-t0iy14xl8",
    "selector": {
      "attributes": {},
      "css": "#textbox[aria-label*=\"Fale sobre seu vídeo\"]",
      "text": "",
      "xpath": "//*[@id=\"textbox\" and contains(@aria-label, \"Fale sobre seu vídeo\")]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508626727,
    "type": "input",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508639363-kfl7gelea",
    "selector": {
      "attributes": {},
      "css": "#next-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"next-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508639363,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508640623-8oii71n3f",
    "selector": {
      "attributes": {},
      "css": "div.m10n-text.clickable.style-scope.ytcp-video-monetization",
      "text": "Selecionar",
      "xpath": "//div[@class=\"m10n-text clickable style-scope ytcp-video-monetization\"]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "Selecionar",
    "timestamp": 1768508640623,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508641598-x8bsygazv",
    "selector": {
      "attributes": {},
      "css": "#offRadio",
      "text": null,
      "xpath": "//*[@id=\"offRadio\"]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508641598,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508642722-xl8ifa96t",
    "selector": {
      "attributes": {},
      "css": "#save-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"save-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508642722,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508645257-vgo3feane",
    "selector": {
      "attributes": {},
      "css": "#next-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"next-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508645257,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508648617-d4nvl8hbe",
    "selector": {
      "attributes": {},
      "css": "#checkbox-container",
      "text": null,
      "xpath": "//*[@id=\"checkbox-container\"]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508648617,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508650701-iikwczgex",
    "selector": {
      "attributes": {},
      "css": "#submit-questionnaire-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"submit-questionnaire-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508650701,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508656083-pnub35itj",
    "selector": {
      "attributes": {},
      "css": "#next-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"next-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508656083,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A",
    "waitBefore": 5000
  },
  {
    "attributes": {},
    "id": "1768508659865-sb583zg96",
    "selector": {
      "attributes": {},
      "css": "#next-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"next-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508659865,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508662880-ll12dwi3h",
    "selector": {
      "attributes": {},
      "css": "#next-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"next-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508662880,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508670826-nr6x947fm",
    "selector": {
      "attributes": {},
      "css": "#second-container-expand-button > yt-icon.remove-defaults > span.yt-icon-shape > div",
      "text": null,
      "xpath": "//*[@id=\"second-container-expand-button\"]/yt-icon/span/div"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508670826,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508673075-i92q050ln",
    "selector": {
      "attributes": {},
      "css": "#datepicker-trigger > ytcp-dropdown-trigger.style-scope > div.container > div.left-container:nth-child(2) > span.dropdown-trigger-text",
      "text": "16 de jan. de 2026",
      "xpath": "//*[@id=\"datepicker-trigger\"]/ytcp-dropdown-trigger/div/div[2]/span"
    },
    "tabId": 394884283,
    "tagName": "span",
    "text": "16 de jan. de 2026",//usar o arquivo que está na planilha google na coluna "Data do Video"
    "timestamp": 1768508673075,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },

  {
    "attributes": {
      "placeholder": ""
    },
    "id": "1768508680999-0u82bel8h",
    "inputType": "text",
    "selector": {
      "attributes": {},
      "css": "#datepicker-trigger",
      "text": null,
      "xpath": "//*[@id=\"datepicker-trigger\"]"
    },
    "tabId": 394884283,
    "timestamp": 1768508680999,
    "type": "input",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A",
    "value": "13/02/2026"
  },
  {
    "attributes": {
      "placeholder": ""
    },
    "id": "1768508690124-013p4kja4",
    "inputType": "text",
    "selector": {
      "attributes": {
        "placeholder": ""
      },
      "css": "#time-of-day-container input",
      "text": null,
      "xpath": "//*[@id=\"time-of-day-container\"]//input"
    },
    "tabId": 394884283,
    "timestamp": 1768508690124,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A",
    "value": "20:00"
  },
  {
    "attributes": {
      "placeholder": ""
    },
    "id": "1768508690124-013p4kja4",
    "inputType": "text",
    "selector": {
      "attributes": {
        "placeholder": ""
      },
      "css": "#time-of-day-container input",
      "text": null,
      "xpath": "//*[@id=\"time-of-day-container\"]//input"
    },
    "tabId": 394884283,
    "timestamp": 1768508690124,
    "type": "input",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A",
    "value": "20:00"
  },
  {
    "attributes": {},
    "id": "1768508692788-msxv57kp2",
    "selector": {
      "attributes": {},
      "css": "tp-yt-iron-overlay-backdrop.opened",
      "text": null,
      "xpath": "//tp-yt-iron-overlay-backdrop[@class=\"opened\"]"
    },
    "tabId": 394884283,
    "tagName": "tp-yt-iron-overlay-backdrop",
    "text": "",
    "timestamp": 1768508692788,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508695833-f4vqlmeg2",
    "selector": {
      "attributes": {},
      "css": "#done-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"done-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508695833,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "attributes": {},
    "id": "1768508698591-1a8mc8pik",
    "selector": {
      "attributes": {},
      "css": "#primary-action-button > ytcp-button-shape > button.ytcpButtonShapeImplHost > yt-touch-feedback-shape.yt-spec-touch-feedback-shape > div.yt-spec-touch-feedback-shape__fill:nth-child(2)",
      "text": null,
      "xpath": "//*[@id=\"primary-action-button\"]/ytcp-button-shape/button/yt-touch-feedback-shape/div[2]"
    },
    "tabId": 394884283,
    "tagName": "div",
    "text": "",
    "timestamp": 1768508698591,
    "type": "click",
    "url": "https://studio.youtube.com/channel/UCA_uN3X9OcuFGAZaxK7Ri7A"
  },
  {
    "id": "1768508710000-closebtn",
    "type": "click",
    "selector": {
      "attributes": {},
      "css": "#close-button button",
      "xpath": "//*[@id=\"close-button\"]//button",
      "text": "Fechar"
    },
    "tabId": 394884283,
    "tagName": "button",
    "text": "Fechar",
    "timestamp": 1768508710000
  }
];

let isReplaying = false;
let currentActionIndex = 0;

// Loop processing variables
let currentRowData = null;
let sheetsManager = null;
let pendingRows = [];
let currentRowIndex = 0;
let config = null;

// Listen for replay commands
chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
  if (request.action === 'startReplay') {
    startReplay(sendResponse);
    return true;
  } else if (request.action === 'startReplayLoop') {
    // Nova ação: processar múltiplas linhas da planilha
    startReplayLoop(request.config, sendResponse);
    return true;
  } else if (request.action === 'stopReplay') {
    stopReplay(sendResponse);
    return true;
  } else if (request.action === 'getStatus') {
    sendResponse({
      isReplaying,
      currentAction: currentActionIndex,
      totalActions: ACTIONS.length,
      currentRow: currentRowIndex,
      totalRows: pendingRows.length
    });
  }
  return true;
});

// Start replaying actions
async function startReplay(sendResponse) {
  if (isReplaying) {
    sendResponse({ success: false, error: 'Already replaying' });
    return;
  }

  isReplaying = true;
  currentActionIndex = 0;

  console.log('Starting replay of', ACTIONS.length, 'actions');
  sendResponse({ success: true });

  try {
    for (let i = 0; i < ACTIONS.length && isReplaying; i++) {
      currentActionIndex = i;
      const action = ACTIONS[i];

      console.log(`Executing action ${i + 1}/${ACTIONS.length}:`, action.type);

      await executeAction(action);

      // Notify background of progress
      chrome.runtime.sendMessage({
        action: 'replayProgress',
        current: i + 1,
        total: ACTIONS.length
      }).catch(() => { });

      // Wait between actions
      await wait(500);
    }

    console.log('Replay completed successfully');
    isReplaying = false;

    chrome.runtime.sendMessage({
      action: 'replayComplete',
      success: true
    }).catch(() => { });

  } catch (error) {
    console.error('Replay error:', error);
    isReplaying = false;

    chrome.runtime.sendMessage({
      action: 'replayComplete',
      success: false,
      error: error.message
    }).catch(() => { });
  }
}

// Stop replaying
function stopReplay(sendResponse) {
  isReplaying = false;
  console.log('Replay stopped');
  sendResponse({ success: true });
}

// ========== LOOP PROCESSING ==========

/**
 * Processa múltiplas linhas da planilha em loop
 * @param {object} receivedConfig - Configuração recebida do popup
 * @param {function} sendResponse - Callback para resposta
 */
async function startReplayLoop(receivedConfig, sendResponse) {
  if (isReplaying) {
    sendResponse({ success: false, error: 'Já está processando' });
    return;
  }

  isReplaying = true;
  config = receivedConfig;
  pendingRows = config.pendingRows || [];

  console.log(`🎬 Iniciando processamento em loop de ${pendingRows.length} vídeo(s)`);

  // Enviar resposta imediata
  sendResponse({ success: true });

  // Inicializar sheets manager (precisa estar no contexto da página)
  if (typeof GoogleSheetsManager === 'undefined') {
    console.error('❌ GoogleSheetsManager não encontrado. sheets-integration.js não foi carregado.');
    notifyError('Erro: GoogleSheetsManager não disponível');
    isReplaying = false;
    return;
  }

  try {
    sheetsManager = new GoogleSheetsManager(config.sheetUrl, config.sheetTab);
    await sheetsManager.validateSheet();

    let successCount = 0;
    let errorCount = 0;
    const errors = [];

    // Loop por cada linha pendente
    for (currentRowIndex = 0; currentRowIndex < pendingRows.length; currentRowIndex++) {
      if (!isReplaying) {
        console.log('⏹ Processamento interrompido pelo usuário');
        break;
      }

      currentRowData = pendingRows[currentRowIndex];

      console.log(`\n📹 [${currentRowIndex + 1}/${pendingRows.length}] Processando:`);
      console.log(`   Linha: ${currentRowData.rowNumber}`);
      console.log(`   Título: "${currentRowData.tituloVideo}"`);
      console.log(`   Arquivo: ${currentRowData.nomeArquivo}`);

      // Notificar progresso
      chrome.runtime.sendMessage({
        action: 'replayProgress',
        current: currentRowIndex + 1,
        total: pendingRows.length,
        videoTitle: currentRowData.tituloVideo,
        status: 'Processando...'
      }).catch(() => { });

      try {
        // 🔍 VERIFICAR SE VÍDEO JÁ ESTÁ AGENDADO
        if (typeof youTubeManager !== 'undefined') {
          const isScheduled = await youTubeManager.checkVideoScheduled(currentRowData.tituloVideo, currentRowData.dataPostagem);

          if (isScheduled) {
            console.log(`⚠️ Vídeo "${currentRowData.tituloVideo}" já agendado. Pulando upload...`);

            // Atualizar status na planilha para evitar reprocessamento futuro
            await sheetsManager.updateStatus(currentRowData.rowNumber, 'Já Agendado');

            chrome.runtime.sendMessage({
              action: 'replayProgress',
              current: currentRowIndex + 1,
              total: pendingRows.length,
              videoTitle: currentRowData.tituloVideo,
              status: 'Já Agendado (Pulado)'
            }).catch(() => { });

            continue; // Pula para o próximo vídeo
          }
        } else {
          console.warn('⚠️ youTubeManager não encontrado. Verificação de agendamento ignorada.');
        }

        // Executar todas as ações para esta linha
        await executeAllActionsForRow();

        // ✅ Sucesso - Atualizar planilha
        console.log(`✅ Vídeo processado com sucesso!`);

        try {
          await sheetsManager.updateStatus(currentRowData.rowNumber, 'Concluído');
          console.log(`   ✓ Planilha atualizada (linha ${currentRowData.rowNumber})`);
        } catch (updateError) {
          console.warn(`   ⚠️ Vídeo postado mas erro ao atualizar planilha:`,
            typeof updateError === 'object' ? JSON.stringify(updateError, Object.getOwnPropertyNames(updateError)) : updateError
          );
          // Não falha o processo, apenas avisa
        }

        successCount++;

      } catch (error) {
        // ❌ Erro no processamento
        console.error(`❌ Erro ao processar vídeo da linha ${currentRowData.rowNumber}:`, error);

        errorCount++;
        errors.push({
          rowNumber: currentRowData.rowNumber,
          videoTitle: currentRowData.tituloVideo,
          error: error.message
        });

        // Notificar erro
        chrome.runtime.sendMessage({
          action: 'replayError',
          rowNumber: currentRowData.rowNumber,
          videoTitle: currentRowData.tituloVideo,
          error: error.message
        }).catch(() => { });

        // Decisão: continuar ou parar?
        // Por padrão: continuar com próximo vídeo
        console.log('   ⏭️ Continuando com próximo vídeo...');
      }

      // Aguardar entre vídeos (evitar detecção de bot)
      if (currentRowIndex < pendingRows.length - 1) {
        const delayMs = 5000 + Math.random() * 3000; // 5-8 segundos
        console.log(`   ⏳ Aguardando ${(delayMs / 1000).toFixed(1)}s antes do próximo...`);
        await wait(delayMs);
      }
    }

    // Finalizado
    isReplaying = false;
    currentRowData = null;

    const message = `Processamento concluído!\n\n✅ Sucesso: ${successCount}\n❌ Erros: ${errorCount}`;

    console.log(`\n🎉 ${message}`);

    if (errors.length > 0) {
      console.log('\n📋 Erros detalhados:');
      errors.forEach(err => {
        console.log(`   - Linha ${err.rowNumber} (${err.videoTitle}): ${err.error}`);
      });
    }

    chrome.runtime.sendMessage({
      action: 'replayComplete',
      success: errorCount === 0,
      message: message,
      stats: { successCount, errorCount, total: pendingRows.length }
    }).catch(() => { });

  } catch (error) {
    console.error('❌ Erro fatal no loop:', error);
    isReplaying = false;

    chrome.runtime.sendMessage({
      action: 'replayComplete',
      success: false,
      message: `Erro fatal: ${error.message}`
    }).catch(() => { });
  }
}

/**
 * Executa todas as ações para a linha atual (currentRowData)
 */
async function executeAllActionsForRow() {
  console.log(`   ▶️ Executando ${ACTIONS.length} ações...`);

  for (let i = 0; i < ACTIONS.length && isReplaying; i++) {
    currentActionIndex = i;
    const action = ACTIONS[i];

    console.log(`      [${i + 1}/${ACTIONS.length}] ${action.type}...`);

    await executeAction(action);

    // Esperar entre ações
    await wait(500);
  }

  console.log(`   ✓ Todas as ações executadas`);
}

// Execute a single action
async function executeAction(action) {
  if (action.waitBefore) {
    console.log(`   ⏳ Waiting ${action.waitBefore}ms before action...`);
    await wait(action.waitBefore);
  }

  console.log(`   🔍 Procurando elemento: ${action.selector.css || action.selector.xpath}`);
  const element = await findElement(action.selector);

  if (!element) {
    console.error(`   ❌ Elemento NÃO encontrado: ${action.selector.css || action.selector.xpath}`);
    throw new Error(`Element not found: ${action.selector.css || action.selector.xpath}`);
  }

  console.log(`   ✅ Elemento encontrado: ${element.tagName} (Classe: ${element.className})`);
  // console.log(`      HTML: ${element.outerHTML.substring(0, 100)}...`);

  // Scroll element into view
  element.scrollIntoView({ behavior: 'smooth', block: 'center' });
  await wait(300);

  // Highlight element
  highlightElement(element);

  // Execute action based on type
  switch (action.type) {
    case 'click':
      element.click();
      break;

    case 'input':
      element.focus();

      // ⭐ SUBSTITUIR VALORES DA PLANILHA
      let value = action.value || '';
      let isFileUpload = false;

      if (currentRowData) {
        // Upload de arquivo (input type="file")
        if (action.inputType === 'file' && action.attributes?.name === 'Filedata') {
          const filePath = buildFilePath(currentRowData.nomeArquivo);
          console.log(`         📁 Iniciando upload via debugger: ${filePath}`);

          // Enviar comando para background (chrome.debugger)
          // Selector deve ser CSS válido
          const selector = action.selector.css || `input[name="${action.attributes.name}"]`;

          let uploaded = false;
          try {
            await new Promise((resolve, reject) => {
              chrome.runtime.sendMessage({
                action: 'uploadFile',
                selector: selector,
                filePath: filePath
              }, (response) => {
                if (chrome.runtime.lastError) {
                  reject(chrome.runtime.lastError);
                } else if (response && response.success) {
                  console.log('         ✅ Arquivo local definido via debugger');
                  resolve();
                } else {
                  reject(new Error(response?.error || 'Erro desconhecido no upload'));
                }
              });
            });
            uploaded = true;
          } catch (uploadError) {
            console.warn(`         ⚠️ Falha no arquivo local: ${uploadError.message}`);
            console.log(`         🔄 Tentando baixar do Google Drive...`);
            
            chrome.runtime.sendMessage({
              action: 'replayProgress',
              current: currentRowIndex + 1,
              total: pendingRows.length,
              videoTitle: currentRowData.tituloVideo,
              status: 'Baixando do Drive (pode demorar)...'
            }).catch(() => { });

            try {
              // 1. Download do Drive via Background Script
              const downloadedPath = await new Promise((resolve, reject) => {
                chrome.runtime.sendMessage({
                  action: 'downloadFromDrive',
                  fileName: currentRowData.nomeArquivo
                }, (response) => {
                  if (chrome.runtime.lastError) reject(chrome.runtime.lastError);
                  else if (response && response.success) resolve(response.filePath);
                  else reject(new Error(response?.error || 'Erro no download'));
                });
              });

              console.log(`         ✅ Download concluído. Novo caminho: ${downloadedPath}`);
              console.log(`         🔄 Retentando upload...`);

              // 2. Tentar upload novamente com o novo caminho
              await new Promise((resolve, reject) => {
                chrome.runtime.sendMessage({
                  action: 'uploadFile',
                  selector: selector,
                  filePath: downloadedPath
                }, (response) => {
                  if (chrome.runtime.lastError) reject(chrome.runtime.lastError);
                  else if (response && response.success) {
                    console.log('         ✅ Arquivo definido com sucesso após download!');
                    resolve();
                  } else {
                    reject(new Error(response?.error || 'Erro no upload após download'));
                  }
                });
              });
              uploaded = true;

            } catch (downloadError) {
              console.error(`         ❌ Erro ao baixar do Drive:`, downloadError);
              throw new Error(`Arquivo local ausente e falha ao baixar do Drive: ${downloadError.message}`);
            }
          }

          if (!uploaded) {
            throw new Error('Falha no upload de arquivo');
          }

          isFileUpload = true;
        }
        // Título do vídeo (ID específico da action)
        else if (action.id === '1768508620921-277cyrlef') {
          value = currentRowData.tituloVideo;
          console.log(`         📝 Título: ${value}`);
        }
        // Descrição do vídeo (ID específico da action)
        else if (action.id === '1768508626727-t0iy14xl8') {
          value = currentRowData.descricaoVideo;
          console.log(`         📄 Descrição: ${value.substring(0, 50)}...`);
        }
        // Data de postagem (Complexo: Click Trigger -> Input in Popup)
        else if (action.id === '1768508680999-0u82bel8h') {
          const dateValue = formatDateForYouTube(currentRowData.dataPostagem);
          console.log(`         📅 Data (Via Popup): ${dateValue}`);

          // 1. Clicar no trigger para abrir o calendário
          if (element) {
            element.click();
            await wait(500); // Esperar popup abrir

            // 2. Procurar o input dentro do popup (geralmente criado no final do body)
            // Tenta seletores comuns do datepicker do YT
            const dateInput = document.querySelector('ytcp-date-picker input') ||
              document.querySelector('tp-yt-paper-input[label*="Data"] input') ||
              document.querySelector('input[aria-label*="Data"]');

            if (dateInput) {
              dateInput.value = dateValue;
              dateInput.dispatchEvent(new Event('input', { bubbles: true }));
              dateInput.dispatchEvent(new Event('change', { bubbles: true }));

              // Tentar simular Enter para fechar/confirmar
              dateInput.dispatchEvent(new KeyboardEvent('keydown', {
                key: 'Enter',
                code: 'Enter',
                which: 13,
                keyCode: 13,
                bubbles: true
              }));
              console.log('         ✅ Data definida no popup');
            } else {
              console.warn('         ⚠️ Input de data não encontrado no popup');
            }
          }
          isFileUpload = true; // Impede o script de tentar escrever no trigger (que é uma div)
        }
        // Horário de postagem (Dinâmico da planilha)
        else if (action.id === '1768508690124-013p4kja4') {
          value = formatTimeForYouTube(currentRowData.dataPostagem);
          console.log(`         ⏰ Horário: ${value}`);
        }

      }

      if (!isFileUpload) {
        if (element.isContentEditable) {
          element.textContent = value;
        } else {
          element.value = value;
        }
        element.dispatchEvent(new Event('input', { bubbles: true }));
        element.dispatchEvent(new Event('change', { bubbles: true }));
      }
      break;

    case 'select':
      element.value = action.value || '';
      element.dispatchEvent(new Event('change', { bubbles: true }));
      break;

    case 'checkbox':
    case 'radio':
      if (element.checked !== action.checked) {
        element.click();
      }
      break;

    case 'submit':
      element.submit();
      break;

    case 'navigate':
      if (action.url && action.url !== window.location.href) {
        window.location.href = action.url;
        await wait(2000); // Wait for navigation
      }
      break;

    default:
      console.warn('Unknown action type:', action.type);
  }
}

// Find element using selector strategies
// Find element using selector strategies with polling (Smart Wait)
async function findElement(selectorObj, timeout = 20000) {
  return new Promise((resolve) => {
    const startTime = Date.now();

    // Check immediately
    const check = () => {
      // 1. Try CSS
      if (selectorObj.css) {
        try {
          const element = document.querySelector(selectorObj.css);
          if (element) return element;
        } catch (e) {
          // Ignore invalid selector errors
        }
      }

      // 2. Try XPath
      if (selectorObj.xpath) {
        try {
          const result = document.evaluate(
            selectorObj.xpath,
            document,
            null,
            XPathResult.FIRST_ORDERED_NODE_TYPE,
            null
          );
          if (result.singleNodeValue) {
            return result.singleNodeValue;
          }
        } catch (e) {
          // Ignore
        }
      }
      return null;
    };

    // First attempt
    const result = check();
    if (result) {
      resolve(result);
      return;
    }

    // Start polling
    const interval = setInterval(() => {
      const el = check();
      if (el) {
        clearInterval(interval);
        resolve(el);
      } else if (Date.now() - startTime > timeout) {
        clearInterval(interval);
        console.warn('Timed out waiting for element:', selectorObj);
        resolve(null);
      }
    }, 200);
  });
}

// Check if element is visible (optional but good for robustness)
function isElementVisible(el) {
  if (!el) return false;
  // Basic check - can be expanded
  return el.offsetParent !== null || (el.style && el.style.display !== 'none' && el.style.visibility !== 'hidden');
}

/**
 * Aguarda um elemento desaparecer (ex: loader)
 */
function waitForLoaderToDisappear(selector, timeout = 10000) {
  console.log(`   ⏳ Aguardando desaparecimento de: ${selector}`);
  return new Promise((resolve) => {
    // Se não existe já, retorna
    if (!document.querySelector(selector)) {
      resolve();
      return;
    }

    const interval = setInterval(() => {
      if (!document.querySelector(selector)) {
        clearInterval(interval);
        console.log("      Loader desapareceu.");
        resolve();
      }
    }, 200);

    setTimeout(() => {
      clearInterval(interval);
      console.warn("      Loader ainda visível após timeout, continuando...");
      resolve();
    }, timeout);
  });
}

// Highlight element during replay
function highlightElement(element) {
  const original = {
    outline: element.style.outline,
    outlineOffset: element.style.outlineOffset
  };

  element.style.outline = '3px solid #38ef7d';
  element.style.outlineOffset = '2px';

  setTimeout(() => {
    element.style.outline = original.outline;
    element.style.outlineOffset = original.outlineOffset;
  }, 800);
}

// Wait utility
function wait(ms) {
  return new Promise(resolve => setTimeout(resolve, ms));
}

// ========== HELPER FUNCTIONS ==========

/**
 * Constrói o caminho completo do arquivo
 * Usa a pasta configurada + nome do arquivo
 */
function buildFilePath(fileName) {
  if (!config || !config.videosFolder) {
    return fileName; // Retorna apenas o nome se não tiver pasta configurada
  }

  // Remover barras extras
  let folder = config.videosFolder.replace(/[\\/]$/, '');

  // Detectar SO (simplificado)
  const isWindows = folder.includes(':') || folder.includes('\\');
  const separator = isWindows ? '\\' : '/';

  return `${folder}${separator}${fileName}`;
}

/**
 * Formata data da planilha (DD/MM/YYYY HH:MM) para formato do YouTube (DD/MM/YYYY)
 */
function formatDateForYouTube(dateTimeStr) {
  if (!dateTimeStr) return '';

  // Esperado: "16/01/2026 20:00" -> "16 de jan. de 2026" (dependendo do locale)
  // Mas como o input do YouTube aceita DD/MM/YYYY direto se for text input:

  const parts = dateTimeStr.split(' ');
  return parts[0]; // Retorna "16/01/2026"
}

/**
 * Extrai horário da data da planilha
 */
function formatTimeForYouTube(dateTimeStr) {
  if (!dateTimeStr) return '20:00'; // Default

  const parts = dateTimeStr.split(' ');
  if (parts.length > 1) {
    return parts[1]; // Retorna "20:00"
  }
  return '20:00';
}

/**
 * Notifica erro ao popup
 */
function notifyError(message) {
  chrome.runtime.sendMessage({
    action: 'replayComplete',
    success: false,
    message: message
  }).catch(() => { });
}
